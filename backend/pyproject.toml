[tool.pytest.ini_options]
# FIX: Use auto mode and explicit loop scope for modern pytest-asyncio
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
python_files = "test_*.py"
filterwarnings = [
    "ignore::DeprecationWarning",
]
